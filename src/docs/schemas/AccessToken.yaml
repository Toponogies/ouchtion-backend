type: object
title: AccessToken
description: Schema for an access token.
properties:
  accessToken:
    type: string
    description: The access token.
required:
  - accessToken
# May be deleted in the future