title: ResetPassword
description: Schema for a reset password request.
type: object
properties:
  password:
    type: string
    description: The new password of a User.
  token:
    type: string
    description: The token gets from email.
required:
  - password
  - token