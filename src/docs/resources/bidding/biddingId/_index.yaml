get:
  operationId: getBidding
  summary: Get a bidding.
  parameters:
    - $ref: "../../../parameters/header/accessToken.yaml"
    - $ref: "../../../parameters/path/id.yaml"
  responses:
    "200":
      description: Got a bidding.
      content:
        application/json:
          schema:
            $ref: "../../../schemas/bidding.yaml"
    "400":
      $ref: "../../../responses/Error.yaml"
    "401":
      $ref: "../../../responses/Error.yaml"
  tags:
    - biddings