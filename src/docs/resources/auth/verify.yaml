post:
  operationId: VerifyEmail
  summary: Verifies email for a User.
  security: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/Token.yaml"
  responses:
    "204":
      description: Checks token and verifies email.
  tags:
    - auth
