post:
  operationId: Register
  summary: Registers a User.
  security: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/Register.yaml"
  responses:
    "204":
      description: Creates a User and waits for email verification.
  tags:
    - auth
